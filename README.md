# React + Vite

## Setup

Clone Repo: `git clone https://github.com/AthulSubashM/athulsubashm.github.io/`
Install packages: `npm install`

Start dev enviroment: `npm run dev`
Run build: `npm run build`
Deploy: npm run deploy
